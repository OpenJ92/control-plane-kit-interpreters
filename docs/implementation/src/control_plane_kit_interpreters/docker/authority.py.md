Source: [src/control_plane_kit_interpreters/docker/authority.py](../../../../../src/control_plane_kit_interpreters/docker/authority.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This owner compares requested authority-delivery shape with observed Docker resource facts. It returns conformant, conflict or unknown; it neither admits authority nor attests a physical host. Exact mounts/groups can conform directly. The special Docker Desktop proxy relation is an explicitly fixed empirical provider/API tuple and requires the intended local socket plus configured and actual proxy observations.

Do not generalize that exception by rewriting raw SDK facts or equating configured intent with actual RW. Missing/malformed observations remain unknown; no result grants new powers. Any extension needs a concrete provider contract and evidence in its owning issue, not another broadly permissive path alias.

Source and governing references: [src/control_plane_kit_interpreters/docker/sdk.py](../../../../../src/control_plane_kit_interpreters/docker/sdk.py), [src/control_plane_kit_interpreters/docker/runtime.py](../../../../../src/control_plane_kit_interpreters/docker/runtime.py), [src/control_plane_kit_interpreters/docker/observer.py](../../../../../src/control_plane_kit_interpreters/docker/observer.py), [tests/test_docker_runtime_effect_observer.py](../../../../../tests/test_docker_runtime_effect_observer.py).
