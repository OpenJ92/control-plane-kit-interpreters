Source: `tests/live_docker_start_node_contract.py`.
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

[Source](../../../tests/live_docker_start_node_contract.py) is an explicitly enabled synthetic integration fixture inside the existing numeric-secret controller. It exercises the actual runtime/SDK combination with one retained data mount and two protected-file subpath mounts. It is not a recovery tool or a production journal implementation. The [creation evidence relation](../../architecture/docker-creation-evidence.md) owns the shared acceptance limits.

`prepare_fixture` decodes pinned product/ABI specimens, changes the product identity, computes its descriptor reference and constructs Core effect requests with exact synthetic grants. Inspect the selected Core constructors before changing inputs: `PublicStaticEnvironmentBinding` rejects secret-shaped names, while `SecretFilePathBinding` belongs to protected file delivery. This fixture adds no public-static environment overrides. The selected image supplies its existing database path and fixed provider ID; the phase checks those defaults before effects. Do not invent a fresh provider ID in the public environment or confuse fresh workspace/grant identities with production custody.

`ProviderContractObservation` wraps only the recipient's high-level create scope and the correlated API create/inspect. It must preserve call count, argument/return identity and the original exception even when bookkeeping fails. The active pull guard raises before calling Docker; an absent pull credential alone would not prohibit anonymous acquisition. Methods are restored when the context exits.

`FixtureJournal` records prospective ownership and authoritative IDs. Its helper records precede follow-up SDK inspection because a helper may have been created even when inspection fails. Cleanup checks full recipient attachment shape without requiring the recipient to be healthy/running; an extra mount, bind or port prevents deletion. It removes only confirmed synthetic containers, unused matching volumes and an unattached matching network. Missing identity or a failed read preserves uncertainty.

The one recipient exec runs as numeric user10006, captures no stdout/stderr and has an in-recipient15s limit plus20s controller deadline and bounded exact-ID polling. It checks dummy-file hashes, uid/mode, denied O_WRONLY opens without writing, and local readiness. Do not replace detached execution with an unbounded streamed read.

Installed Core, SDK/runtime bytes, Docker version and image identity/default checks are deliberate comparison prerequisites. The ordinary gate leaves this phase off; enabling it requires the reviewed new identity/record inputs and exact synthetic cleanup authority. [ProviderContractObservationTests](../../../tests/test_docker_sdk_client.py) covers noninterference, input construction and attachment-safe cleanup. Those tests are not the real provider witness. Historical [#141](https://github.com/OpenJ92/control-plane-kit-interpreters/issues/141) establishes why both evidence classes exist.
